<PaperCard as |Card|>
  <PaperToolbar as |Toolbar|>
    <Toolbar.tools>
      <h2>Ideas</h2>
      <span class="flex"></span>
      <PaperButton @onClick={{action (mut this.isAdding) true}}>
        Add
      </PaperButton>
    </Toolbar.tools>
  </PaperToolbar>
  {{#if this.isAdding}}
    <PaperForm @onSubmit={{this.createIdea}} as |Form|>
      <Card.content>
        <Form.input
          @textarea={{true}}
          @block={{true}}
          @label="New Idea"
          @value={{this.summary}}
          @onChange={{action (mut this.summary)}}
          @passThru={{hash autocapitalize="sentences"}}
        />
      </Card.content>
      <Card.actions>
        <PaperButton @onClick={{action (mut this.isAdding) false}}>
          Cancel
        </PaperButton>
        <Form.submit-button
          @raised={{true}}
          @primary={{true}}
        >
          Save
        </Form.submit-button>
      </Card.actions>
    </PaperForm>
  {{/if}}
</PaperCard>
