<PaperCard as |Card|>
  <Card.title as |Title|>
    <Title.text as |Text|>
      <Text.headline>{{@author.name}}</Text.headline>
    </Title.text>
  </Card.title>
  <Card.actions>
    <PaperButton @warn={{true}} @onClick={{this.delete}}>
      Delete
    </PaperButton>
    <NavButton @to="authors.detail" @id={{@author.id}}>
      Details
    </NavButton>
  </Card.actions>
</PaperCard>
