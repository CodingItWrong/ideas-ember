<PaperCard>
  <PaperList>
    {{#each this.sortedSources as |source|}}
      {{#if (has-block)}}
        <SourceItem @source={{source}}>
          {{yield source}}
        </SourceItem>
      {{else}}
        <SourceItem @source={{source}} />
      {{/if}}
    {{/each}}
  </PaperList>
</PaperCard>
