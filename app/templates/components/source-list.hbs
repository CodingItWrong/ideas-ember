<PaperCard as |Card|>
  <PaperList>
    {{#each this.sortedSources as |source|}}
      <SourceItem @source={{source}}>
        {{source.author.name}}
      </SourceItem>
    {{/each}}
  </PaperList>
</PaperCard>
