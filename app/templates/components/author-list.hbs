{{#if this.sortedAuthors}}
  <PaperCard>
    <PaperList>
      {{#each this.sortedAuthors as |author|}}
        <AuthorItem @author={{author}} />
      {{/each}}
    </PaperList>
  </PaperCard>
{{/if}}
