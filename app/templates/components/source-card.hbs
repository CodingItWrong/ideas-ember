<PaperCard as |Card|>
  <Card.title as |Title|>
    <Title.text as |Text|>
      <Text.headline>{{@source.title}}</Text.headline>
    </Title.text>
  </Card.title>
  <Card.actions>
    <PaperButton @warn={{true}} @onClick={{this.delete}}>
      Delete
    </PaperButton>
    <NavButton @to="sources.detail" @id={{@source.id}}>
      Details
    </NavButton>
  </Card.actions>
</PaperCard>
