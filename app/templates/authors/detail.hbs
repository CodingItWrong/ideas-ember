<BackButton @to="authors" />

<AuthorHeader @author={{this.model}} />

<NewSourceForm @author={{this.model}} />

<SourceList @sources={{this.model.sources}} as |source|>
  {{source.date}}
</SourceList>
