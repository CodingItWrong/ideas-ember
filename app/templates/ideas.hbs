<BackButton @to="index" />

<PaperCard as |Card|>
  <Card.title as |Title|>
    <Title.text as |Text|>
      <Text.headline>{{this.model.summary}}</Text.headline>
    </Title.text>
  </Card.title>
</PaperCard>

{{#each this.model.quotes as |quote|}}
  <QuoteCard @quote={{quote}} />
{{/each}}
