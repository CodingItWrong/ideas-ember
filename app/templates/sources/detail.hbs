<BackButton @to="authors.detail" @id={{this.model.source.author.id}} />

<Source::Header
  @source={{this.model.source}}
  @media={{this.model.media}}
/>

<SourceTagList
  @source={{this.model.source}}
  @tags={{this.model.tags}}
/>

<Quote::List
  @quotes={{this.model.source.quotes}}
  @ideas={{this.sortedIdeas}}
  @source={{this.model.source}}
/>
