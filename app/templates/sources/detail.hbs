<BackButton @to="authors.detail" @id={{@model.source.author.id}} />

<Source::Header
  @source={{@model.source}}
  @media={{@model.media}}
/>

<SourceTagList
  @source={{@model.source}}
  @tags={{@model.tags}}
/>

<Quote::List
  @quotes={{@model.source.quotes}}
  @ideas={{this.sortedIdeas}}
  @source={{@model.source}}
/>
