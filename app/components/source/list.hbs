<PaperCard as |Card|>
  {{#if this.sortedSources}}
    <PaperList>
      <PaperItem class="md-2-line">
        <div class="form-group">
          <Input
            @value={{this.filter}}
            class="form-control"
            placeholder="Search"
            autocapitalize="sentences"
          />
        </div>
      </PaperItem>
      {{#each this.filteredSources as |source|}}
        {{#if (has-block)}}
          <Source::ListItem @source={{source}}>
            {{yield source}}
          </Source::ListItem>
        {{else}}
          <Source::ListItem @source={{source}} />
        {{/if}}
      {{/each}}
    </PaperList>
  {{else}}
    <Card.content>
      <p>No sources added yet.</p>
    </Card.content>
  {{/if}}
</PaperCard>
